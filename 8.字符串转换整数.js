/**
 * @param {string} str
 * @return {number}
 */
var myAtoi = function(str) {
    if (!isNumber(s[0])) return 0
    const strLen = str.length
    let isNegative = s[0] == '-' ? true : false 
    for (let i = 0; i < strLen; i++) {
        
    }
};

var isNumber = function(s) {
    if (s>='0' && s<='9') return true
    return false
}